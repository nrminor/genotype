{
  "name": "genotype-consumer-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "bunx tsc --noEmit -p tsconfig.json",
    "runtime:tabular": "bun run runtime-tabular.ts",
    "runtime:parquet": "bun run runtime-parquet.ts",
    "runtime:engine-native": "bun run runtime-engine-native.ts",
    "runtime:engine-wasm": "bun run runtime-engine-wasm.ts",
    "check": "bun run typecheck && bun run runtime:tabular && bun run runtime:parquet"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^6.0.3"
  }
}
